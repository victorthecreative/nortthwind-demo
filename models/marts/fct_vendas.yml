version: 2

models:
  - name: fct_vendas
    description: Esta é a dimensão com os dados de vendas
    columns:
      - name: sk_venda
        description: Essa é a chave surrogate key da tabela.
        tests:
          - unique
          - not_null
